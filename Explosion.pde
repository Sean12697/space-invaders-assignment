class Explosion {
  PVector pos;
  float scale;
  int life;
  int lifeSpan = 20;
  PImage images[];
  
  Explosion(PVector pos, float scale) {
    this.pos = pos;
    this.scale = scale;
  }
  
  void render() {
    
  }
}